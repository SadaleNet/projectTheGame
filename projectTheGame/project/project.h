#pragma once

#include <windows.h>

//TODO: Global variables are said to be EVIL! Better to move these to somewhere else. NOTE: Currently, these variables are defined in project.cpp
extern HWND hWnd;
extern HDC hdc;
extern HACCEL hAccelTable;
